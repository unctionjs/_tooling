#!/usr/bin/env bash

bin/unctionjs update --name=@unction/.+ &&
bin/unctionjs add --paths=package.json,package-lock.json &&
bin/unctionjs commit --message='[ignore] cascading upgrade' &&
bin/unctionjs version --bump=minor &&
bin/unctionjs publish
